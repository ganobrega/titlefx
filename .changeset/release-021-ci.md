---
"titlefx": patch
---

Automate releases with GitHub Actions: Changesets opens the version PR, `package.json` changes on `main` trigger an optional `v*` tag when missing, and npm publish runs on tag push via OIDC. Update Changesets README for the new flow.
